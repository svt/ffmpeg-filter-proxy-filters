# Pin a portable x86_64 baseline so brew source builds (e.g. on AMD hosts)
# don't bake in SSE4a / AVX-only instructions and SIGILL on Intel runtimes.
[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=x86-64"]
